<% provide(:title, "Post") %>
<h1>シフト提出</h1>
<%= form_with model: @post, url: '/posts', local:true do |f| %>
    <h4>日付</h4>
    <%= f.text_field :date %>
    <h4>時間</h4>
    <%= f.text_field :time %>
    <%= f.submit '提出' %>
<% end %> 
